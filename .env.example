# ── Analytics (optional) ────────────────────────────────────────────────
# Google Analytics 4 measurement ID (e.g. G-XXXXXXXXXX).
# Loaded only after the visitor grants cookie consent.
NEXT_PUBLIC_GA_ID=

# Privacy-friendly alternative (e.g. Plausible domain). Optional.
NEXT_PUBLIC_PLAUSIBLE_DOMAIN=
# Contact form SMTP (server-only)
SMTP_HOST=smtp.hostinger.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=info@nexolvetechnologies.com
SMTP_PASS=
CONTACT_TO_EMAIL=info@nexolvetechnologies.com
